import React, { Component } from 'react';

import {
  HeaderWrapper,
} from '../styled';

class Header extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <HeaderWrapper>
        Note
      </HeaderWrapper>
    );
  }
}

export default Header;
